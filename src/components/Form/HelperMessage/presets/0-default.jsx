import React from 'react';
import HelperMessage from '../HelperMessage'

export default (
    <HelperMessage
        uxpId="form-helper-message"
    >You can use letters, numbers and periods.
    </HelperMessage>
)