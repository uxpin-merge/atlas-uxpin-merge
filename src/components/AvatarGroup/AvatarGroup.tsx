import React from 'react';
import AvatarGroupM, { AvatarGroupProps } from '@atlaskit/avatar-group';

function AvatarGroup(props: AvatarGroupProps) {
    return <AvatarGroupM {...props}/>
}

export default AvatarGroup;
