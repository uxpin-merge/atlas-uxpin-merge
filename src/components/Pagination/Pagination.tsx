import React from 'react';
import PaginationM, { PaginationPropTypes } from '@atlaskit/pagination';

function Pagination(props: PaginationPropTypes) {
    return <PaginationM {...props}/>
}

export default Pagination;
